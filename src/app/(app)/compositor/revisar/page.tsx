import { RevisarView } from "@/components/Compositor/RevisarView";

// Modo Studio — já está sob o CompositionProvider do compositor/layout.
export default function RevisarPage() {
  return <RevisarView mode="studio" />;
}
